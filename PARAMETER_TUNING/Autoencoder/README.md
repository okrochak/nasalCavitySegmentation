# Hyperparameter Tuning for the Autoencoders on JURECA-DC

1. run create_ray_env.sh to create an environment that contains Ray Tune
2. run the ray_tune_AT_startscript.sh to run the hyperparameter tuning

The script will create a .json file with the parameters and the train and test loss achieved. 
